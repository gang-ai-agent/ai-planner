# Qdrant Indexing

Collections: `travel_knowledge_chunks`, `user_semantic_memories`, and optional `itinerary_embeddings`. Qdrant is a rebuildable search index; PostgreSQL remains the source of truth.
