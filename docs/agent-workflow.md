# Agent Workflow

The workflow runs input guardrails, memory retrieval, intent extraction, prompt building, planning, Qdrant retrieval, context sanitization, reasoning, validation, critique, output guardrails, response generation, and memory write decisions.
